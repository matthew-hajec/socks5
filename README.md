# Elixir SOCKS5 Server

Quick and extensible SOCKS5 server.

## Features

- Listen on a specified IP and port
- Supports DNS resolution by default
- Config for modifying connection and forwarding behavior.